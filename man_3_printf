.TH man_3_printf "09 November 2021" "0.1" "_printf man page"
.SH NAME
B._printf
.PP
.SH DESCRIPTION
Is a custom implementation of the C programming function printf.
.PP
.SH SYNOPSIS 
_printf("%s\n", 'This is a string.');
_printf("The first letter in the alphabet is %c\n", 'A');
.PP
.SH OPTIONS
B. %
followed with (c, s, %, d, i, p, string...)
.PP
.SH FILES
.B _printf.c:
 - contains the function _printf, which uses the prototype 
int _printf(const char *format, ...);. The format string is composed of zero or more directives.
.PP
.B _putchar.c:
 - contains the function _putchar, which writes a character to stdout.
.PP
.B main.h:
 - contains all function prototypes used for _printf.
.PP
.B man_3_printf:
 - manual page for the custom _printf function.
.PP
.B print_chars.c:
 - contains the functions print_c, print_s, print_S, and print_r which handle 
the conversion specifiers c, s, S, and r, respectively, as well as hex_print, which prints 
a char's ascii value in uppercase hex
.PP
.B print_hex.c:
 - contains the functions print_hex, which prints an unsigned int in hexidecimal 
form, print_x, print_X, and print_p, which handle the conversion specifiers x, X, and p, 
respectively
.PP
.B print_unsigned_int.c:
 - contains the functions print_u, print_o, and print_b, which handle the 
conversion specifiers u, o, and b, respectively
.PP
.B print_rot13.c
 - contains the function print_R, which handles the conversion specifier R
.PP
SH: AUTHORS
.PP
Bryan Freire
.PP
Imanol Asolo @Imanolasolo